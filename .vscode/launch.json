{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Makefile",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/prog.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build"
        }
    ],
    
}
